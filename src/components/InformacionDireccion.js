import React from 'react';

function InformacionDireccion({ direccionSeleccionada }) {
  // Puedes definir aquí el contenido similar a lo que hicimos en `Reclamos.js`

  return (
    <div id="informacionDireccion">
      <div className="row">
        {/* Aquí puedes agregar componentes o tablas para Productos, Adicionales, etc. */}
      </div>
    </div>
  );
}

export default InformacionDireccion;
